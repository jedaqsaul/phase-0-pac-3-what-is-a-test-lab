const name = "Susan"; // Changed from "Joe" to "Susan"
const height = 30; // Changed from 74 to a value less than 40
const message = `${name} is ${height} inches tall`;

module.exports = { name, height, message };
